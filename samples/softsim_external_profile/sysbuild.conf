# Partitioning comes from the devicetree (boards/*.overlay including the
# dts/softsim/ layouts). To build with the deprecated Partition Manager
# instead, pass -DSB_CONFIG_PARTITION_MANAGER=y.

SB_CONFIG_SOFTSIM_BUNDLE_TEMPLATE_HEX=y
