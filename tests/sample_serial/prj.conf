CONFIG_ZTEST=y
CONFIG_ASSERT=y

# The unit under test is a UART interrupt callback; drive it through the
# emulated UART declared in boards/native_sim.overlay.
CONFIG_SERIAL=y
CONFIG_UART_INTERRUPT_DRIVEN=y
CONFIG_EMUL=y
