name: softsim

build:
  cmake: .
  kconfig: ./Kconfig
  sysbuild-cmake: sysbuild
  sysbuild-kconfig: sysbuild/Kconfig
  settings:
    dts_root: .
  depends:
    - trusted-firmware-m

samples:
  - samples

tests:
  - tests
